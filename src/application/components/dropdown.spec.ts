describe('Dropdown Component', () => {
    it('should return 2 when 1 + 1', () => {
        // Given
        const a = 1;
        const b = 1;

        // When
        const result = a + b;

        // Then
        expect(result).toBe(3);
    });
});
